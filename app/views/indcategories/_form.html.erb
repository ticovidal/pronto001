<%= form_with(model: indcategory, local: true) do |form| %>
  <% if indcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(indcategory.errors.count, "error") %> prohibited this indcategory from being saved:</h2>

      <ul>
      <% indcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
