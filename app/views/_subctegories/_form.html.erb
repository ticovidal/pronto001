<%= form_with(model: subctegory, local: true) do |form| %>
  <% if subctegory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subctegory.errors.count, "error") %> prohibited this subctegory from being saved:</h2>

      <ul>
      <% subctegory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
